[build]
# Dockerfile is at backend/Dockerfile, but build context must be repo root
# for COPY of sibling dirs (btc_prediction, dart_token)
dockerfilePath = "backend/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
